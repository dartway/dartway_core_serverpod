class: DwUserPassword
table: dw_user_password
fields:
  userId: int
  passwordHash: String
  createdAt: DateTime
  updatedAt: DateTime
indexes:
  dw_user_password_user_id_idx:
    fields: userId
    unique: true