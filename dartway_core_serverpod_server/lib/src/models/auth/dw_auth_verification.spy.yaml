class: DwAuthVerification
table: dw_auth_verification
fields:
  dwAuthRequestId: int, relation(parent=dw_auth_request)
  createdAt: DateTime, default=now
  verificationCode: String, !persist

  accessToken: String?
